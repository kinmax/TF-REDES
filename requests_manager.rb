class RequestsManager
    def initialize(nodes, routers, source, destination, message)
        @nodes = nodes
        @routers = routers
        @source = source
        @destination = destination
        @message = message
        @requests_list = []
    end

    def make_requests
        # TO DO
    end
end